server:
  port: 8085

spring:
  application:
    name: auth-service
  config:
    import: optional:configserver:http://localhost:8888#
  datasource:
    url: jdbc:mysql://localhost:3306/quickserve_auth
    username: root
    password: Chinmay@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmil.com
    port: 857
    username: chaudharichinmay227@gmail.com
    password: zrga ttpv hgor vfgu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

